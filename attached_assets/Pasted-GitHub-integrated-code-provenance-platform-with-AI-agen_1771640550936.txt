GitHub-integrated code provenance platform with AI agent APIs, self-attesting its own codebase as test data to demonstrate tamper-evident receipts for both human and AI-generated code
I'll include the following features:
Home page explaining code provenance with live demo showcasing ForgeProof's own self-attestation as the featured example
GitHub repository browser for selecting repos, branches, commits, or files to attest with one-click workflow
AI Agent Integration API: POST /api/agent/attest endpoint accepting code artifacts with metadata (model, provider, prompt_hash, generation_timestamp)
Agent API key management UI: developers generate API keys to authenticate their AI tools with ForgeProof
Standardized attestation request format for AI agents: {code, language, model_provider, model_name, model_version, prompt_signature, agent_metadata}
Multi-file/directory attestation with deterministic file_hashes[] sorted by path and bundle_hash from canonical manifest
SHA-256 artifact hashing and Ed25519 signing with system keypair auto-generated on first run
Tamper-evident PostgreSQL ledger with prev_entry_hash chaining and entry_hash computed from canonical JSON
Rich metadata tracking: project, model provider/name/version, hosting jurisdiction, policy tags, git commit SHA, repository URL, AI generation context
Policy profile enforcement (US-only models, approved model lists, banned providers) with PASS/FAIL evaluation and detailed reason codes
Verify page with automatic artifact re-fetch from GitHub or manual paste/upload, supporting both human-uploaded and AI-generated code
Ledger browser with search/filtering by source (GitHub vs AI-generated), model, jurisdiction, project, and pagination
Entry detail page showing full metadata, signature, hashes, chain context, AI generation context (if applicable), and linked GitHub commit
Downloadable ForgeProof Receipt v1 JSON with versioned schema: bundle_hash, file_hashes[], signature, public_key, key_id, claims, policy_evaluation, ai_metadata
Evidence pack export (zip: receipt.json, manifest.json, summary.md, policy.json, ai_context.json) for compliance workflows
POST /api/attest endpoint supporting GitHub repo URLs and direct artifact uploads from human users
POST /api/verify endpoint with GitHub re-fetch capability and comprehensive validation (signature, hash, chain, policy)
GitHub webhook receiver for automatic attestation on push events (opt-in per repository)
AI agent SDK examples and documentation: OpenAI function calling, Claude tool use, REST API curl examples, TypeScript/Python snippets
Professional UI with syntax highlighting, copy buttons, visual chain explorer, AI generation badges, and clear PASS/FAIL indicators
Database seed script (npm run seed) that attests the ForgeProof codebase itself, marking it as generated by Replit Agent 3 with complete file manifest
Test suite using the self-attestation entry as test fixtures for verification, receipt generation, and chain validation workflows
Special 'Dogfooding' badge on the self-attestation entry showing ForgeProof proving its own provenance
About page displaying ForgeProof's own attestation receipt with complete metadata and downloadable evidence pack as proof-of-concept
